# DECISION-LOG

- 2026-08-25 · 브랜드 표기는 문다온 (고급 브랜드 전환) — 금강이지스는 폴더/운영명
- 2026-08-25 · 3D는 프로시저럴 파라메트릭 (GLB 수작업·photogrammetry 비채택: 커스텀 재작업 비용)
- 2026-08-25 · 물리엔진 비채택 — 문은 자유도 1, 구속 파라메트릭 애니메이션
- 2026-08-25 · 패턴 "학습" = 분할 그리드 데이터 등록 (ML 비채택: 표본 수십 장으로 재현 불가)
- 2026-08-25 · AR: 아이폰 USDZExporter→Quick Look(유리는 opacity 스왑 후 export), 안드 model-viewer. 마커 AR 비채택
- 2026-08-25 · 데이터 SSOT = md frontmatter 카드 → Zod → TS 생성물 (JSON 직접 import 비채택)
- 2026-08-25 · v1 경계: 견적·로그인·발주 제외, URL 해시 공유 포함, 확장 가능 구조 유지
- 2026-08-25 · 캡처 산출은 자동 다운로드가 아니라 완료 모달(제스처 저장) — Chrome 무제스처 다운로드 차단 실측
- 2026-08-25 · 캡처 배출 포맷 = mp4 단일 (MediaRecorder webm 제거, WebCodecs+mp4-muxer 직접 인코딩 — 재생시간 메타·30fps 보장)
- 2026-08-26 · 패턴 문법 v2 = 문짝 정규화 앵커+반경 arc(+invert 보수영역·center 타원)·스팬드럴·패널별 패턴 — 셀 앵커 좌표계 비채택 (수렴 5R, 킬-실험 5/5)
- 2026-08-26 · 3연동 겹침폭 30mm approx — 60mm는 이음매 이중바 유발(실렌더 QA), 실측치는 문다온 확인 대기
- 2026-08-26 · 공개 배포 선행 (주인님 결정) — 브랜드 게재 권리 확인은 잔여 플래그 유지, 문제 시 비공개 전환
- 2026-08-26 · Vercel 빌드 = 레포 루트(카드 게이트 포함 재현) — 정적 dist 업로드 방식 비채택 (검증 우회 방지)
- 2026-08-26 · ABS 유리 형상(octagon)·3연동 문 전체 아치는 v3 격리 — 현행 문법 밖, 수요 확인 후
- 2026-08-26 · 주문가능성 제약 = 제품 카드 allowlist(glassIds·colorIds·colorCats) + UI 필터 + 로드/전환 시 자동 보정 — 코드 하드코딩 비채택 (카드가 정책 SSOT)
- 2026-08-26 · 공유 링크 신뢰 원칙: URL 주입값도 allowlist 보정을 통과해야 렌더 — UI 필터만으로는 불충분 (실측: 주입 우회 재현 후 차단)
- 2026-08-27 · 공유 링크 검증은 2단 — **존재 검증(카드에 있는 id인가·수치가 유한한가)이 앞단, allowlist 보정(허용 조합인가)이 뒷단.** 앞단 부재로 없는 productId 링크가 앱을 백지로 만들었다(감사 D1). 오염된 필드만 떨구고 나머지는 보존 — 통째 폐기는 카드 id 하나 바뀔 때 고객 선택 전체를 날린다
- 2026-08-27 · 테스트 러너 = Node 내장 `node --test` + 네이티브 타입 스트리핑 (Vitest·Jest 비채택: 검증 1종에 런타임 의존성을 새로 들일 이유가 없음). 대가는 CI Node 24 고정 — Vercel 빌드는 `tsc`만 쓰므로 무영향
- 2026-08-27 · 신규 팜플렛(6p) 반영 범위 = 손잡이·유리·색상 카드 + 스윙 치수/힌지까지. **유리 혼합(모루+투명 등) 비채택** — 문 1장에 유리 2종은 패널별 유리 분리가 필요해 현행 문법(문 단위 glassId) 밖. 수요 확인 후 v3
- 2026-08-27 · 스윙 "라인반달 300/900"은 신규 카드로 만들지 않고 기존 접착식(0.3)·일체형(0.9)에 귀속 — 길이·색상이 일치해 동일 부품군 추정(approx). 문다온 확인 대상
- 2026-08-27 · 조합 제약의 두 번째 축 = **유리 → 손잡이**(모루·굴곡 선택 시 일체형 고정). 제품 allowlist와 같은 패턴으로 유리 카드에 `requiresIntegratedHandle` 플래그를 두어 카드가 정책 SSOT를 유지 — App.tsx 하드코딩 비채택
- 2026-08-27 · 씬 배치 SSOT = `apps/web/src/scene/props.data.ts`(three/JSX 의존 0) — 렌더러 파일(`sceneProps.tsx`) 동거 비채택. 좌표를 `node --test`가 직접 읽어야 "만들었나"가 아니라 **"화면에 보이나"**를 재는 게이트가 성립한다. 카메라(position·target·fov)도 여기로 올려 App.tsx 하드코딩 제거
- 2026-08-27 · 프레임 게이트 판정 = **카메라 절두체 투영(ndc)** — 각도 프록시(총 30°·세로 19°) 비채택. 구면 각도로는 가로 34°·세로 22.5°인 직사각 절두체를 그릴 수 없어 화면 안(ndc −0.82)인 창을 FAIL로 떨어뜨렸다. 의도적 프레임 밖은 `offFrame: 'cutoff' | 'light'` 사유 필드로 면제하되 **화면 안인데 면제면 FAIL**(낡은 면제 표시 제거 강제)
- 2026-08-27 · 기본 카메라 `target.y` 1.15 → **0.98** — 카메라 1.5m·세로 반각 22.5°에서는 시선축이 5.2°만 내려가 바닥이 3.3m 밖일 때만 프레임에 든다. 거실 깊이가 3.5m라 러그를 어디 깔아도 안 보이는 기하학적 벽이었다. 남아도는 천장을 내주고 바닥을 얻는 교환(문 상단 여유 2.3° 잔존)
- 2026-08-27 · ㄱ자(개방형) 소품 숨김 = **좌표 규칙**(`inRemovedCornerWall`: `x > doorW/2+0.04 && z < 0.2`) — 소품 id 열거 비채택. `?edit=1`로 배치를 자주 바꾸므로 id 목록은 즉시 낡는다. 다른 중문에선 조건이 꺼져 자동 복귀하고, 그 복귀를 테스트가 보증한다
- 2026-08-27 · 커튼·창 속커튼(sheer) **비채택** — 저폴리 상자로는 '천'이 안 나오고 벽과 같은 값으로 뭉갰다(실렌더 2라운드). 같은 이유로 담요도 팔걸이 걸침형 폐기 → 좌판 위 접은 천. 천 표현이 필요하면 img2threejs 정밀화 대상이지 프리미티브 조합 대상이 아니다
- 2026-08-27 · 소품 배치 정본 = **주인님 `?edit=1` 실배치** — 게이트 수치가 배치를 이기지 않는다. 게이트는 "사유 없이 프레임 밖으로 샌 것"만 잡고, 의도한 이탈은 데이터에 사유로 남긴다
- 2026-08-27 · AR 앵커 = **벽(vertical)** — three USDZExporter의 기본값이 `horizontal`이라 옵션을 안 주면 중문이 바닥에 눕는다. 실측으로 확정: 같은 씬을 옵션 유무로 export해 usda를 비교하니 `planeAnchoring:alignment`가 `vertical` vs `horizontal`로 갈렸다. 안드로이드는 model-viewer `ar-placement="wall"`. 규약상 −Z가 벽에 붙는데 씬 카메라가 +z에서 문을 보므로 방향이 맞다(model-viewer 실렌더에서 정면 확인)
- 2026-08-27 · AR 세션 안 HUD **비채택** — Quick Look·Scene Viewer는 OS 앱이라 웹 UI가 개입할 수 없고, DOM Overlay가 되는 WebXR은 안드로이드 전용이라 아이폰 손님을 버리게 된다. 대신 **진입 전 정렬 가이드**(실측 폭·높이 + 3단계 안내)를 양 OS 공통으로 둔다 — AR에 대해 말할 수 있는 유일한 자리다
- 2026-08-27 · 인앱브라우저에서 AR **차단 + 안내** — iOS 인앱(WKWebView)은 Quick Look을 실행하지 못하면서 에러도 없이 USDZ를 깨진 글자로 보여준다(iOS 15 회귀, Apple 포럼 690051 미해결). 카톡·라인은 자동 탈출(`kakaotalk://web/openExternal`·`openExternalBrowser=1`), 나머지는 메뉴 안내. 안드로이드 `intent://` 일반 탈출은 **비채택** — intent 문법이 `#`를 먹어 해시에 실린 구성이 통째로 날아간다
- 2026-08-27 · 시점 이탈 방지(감사 D2) = **각도·거리 한계 + 팬 끄기 + 복구 버튼** 3종. 한계의 상한은 임의값이 아니라 **천장**이 정한다(카메라 높이 = `target.y + 거리·cos(polar)`) — 그래서 `minPolarAngle`과 `maxDistance`는 한 쌍으로 `props.data.ts ORBIT`에 함께 둔다. 팬 비채택: 타깃이 문을 벗어나면 회전만으로는 복구가 안 되고 쇼룸에서 타깃을 옮길 이유가 없다
- 2026-08-27 · 시점 초기화 구현 = OrbitControls **`saveState()`/`reset()`** — 카메라 `position`을 직접 써넣는 방식 비채택. drei가 매 프레임 도는 `controls.update()`와 싸워 되돌아오지 않는다(콘솔 계측으로 확인). `target0`은 생성자에서 (0,0,0)으로 잡히므로 drei가 `target` prop을 적용한 **뒤** 마운트 이펙트에서 `saveState()`를 다시 불러야 한다
- 2026-08-27 · 모바일 카메라 = **fov 54(세로)** — 카메라 뒤로 빼기 비채택(거실 깊이 3.5m라 방 밖으로 나간다). three의 fov는 세로라 화면비가 0.89로 좁아지면 가로 시야가 그대로 깎여(반각 36.1°→20.2°) **문 4모서리가 |ndc| 0.90까지 밀렸다**. fov 54로 0.73. 62까지 올리면 0.62지만 광각 왜곡이 제품 비례를 흐린다 — 제품 앱에서 왜곡은 무료가 아니다
- 2026-08-27 · 공유 카드 이미지 허용 호스트 = **우리 블롭 저장소 하나와 정확 일치**(감사 D3). 저장소 id는 R/W 토큰 4번째 세그먼트에서 유도하고, 못 구하면 외부 이미지를 통째로 거부한다(fail-closed) — 접미사 매칭(`endsWith`) 비채택: `.public.blob.vercel-storage.com`은 Vercel **고객 전체**가 공유하는 도메인이라 무료 계정 하나로 우리 도메인 OG 카드를 도용할 수 있었다
- 2026-08-27 · `/api/share` 방어 = **same-site 오리진 + 형식 + 크기 + 빈도** 4중. 진짜 인증 비채택 — 공개 앱이라 누구나 토큰을 받을 수 있어 인증이 아무것도 막지 못한다. 목표를 "브라우저에서 우리 사이트를 통해 온 요청만"으로 낮추고 그걸 확실히 한다. 레이트리밋은 인스턴스 로컬(서버리스라 분산 한도가 아님) — 단일 클라이언트 폭주 안전판이며, 진짜 한도가 필요해지면 공유 저장소로 올린다
- 2026-08-27 · 첫 로딩 백지(감사 D4) 대응 = **두 겹**. 번들 다운로드 구간은 `index.html` 인라인 `#boot`(React가 아직 없으므로 JS로는 못 덮는다), 텍스처 로드 구간은 React 오버레이. 스피너 한 개로는 앞 구간을 못 덮는다는 게 핵심 — 로딩 표시는 **번들보다 먼저** 그려져야 한다. 텍스처는 `TextureLoader` 명령형이라 Suspense가 못 잡아 `THREE.DefaultLoadingManager`로 집계한다
- 2026-08-27 · 블롭 호스트 비교는 **소문자 정규화 후 정확 일치**. 토큰의 store id는 대소문자 혼합인데 실제 호스트·`URL.hostname`은 소문자라, 눕히지 않으면 fail-closed가 '항상 닫힘'으로 굳어 기능이 조용히 죽는다 — 배포 후 운영 검증에서 실제로 그렇게 났다. 교훈: **fail-closed 설계는 '열리는 경로'를 반드시 실환경에서 한 번 통과시켜 봐야 한다**(닫히는 것만 확인하면 통과)
- 2026-08-27 · 감사 D6(공유 카드가 없는 기능 약속) = **문구 유지, 구현으로 맞춘다**(주인님 결정). R1-15의 "견적 v1 제외"를 갱신 — 제외가 아니라 후속 범위다. 문구를 낮추는 안 비채택. **단 출시 전까지는 과약속이 남아 있고, 착수 블로커는 코드가 아니라 단가 데이터다** — 카드 스키마(색상·유리·패턴·손잡이·제품)에 가격 필드가 0개이고 팜플렛에도 가격이 없다(GRILL R1-15에 이미 기록)
- 2026-08-27 · 하부레일(문턱) = **카드 종류 신설**(`data/cards/rails/`) — 제품 카드에 `railOptions: [7, 20]` 같은 숫자 배열로 박는 안 비채택. 레일은 이름·트레이드오프 문구·높이를 함께 가진 선택지라 유리·손잡이와 같은 급이고, 카드로 두면 기존 allowlist·교차검증·해시 보정 경로를 그대로 재사용한다. 숫자 배열이었으면 "20mm는 발에 걸린다"를 적을 자리가 없다
- 2026-08-27 · 레일 없는 제품은 `railIds` **필드 자체를 두지 않는다**(빈 배열 아님) — 교차검증이 빈 배열을 FAIL로 떨어뜨린다. 빈 배열은 "레일이 없다"가 아니라 "고를 게 없다"라서 UI가 빈 섹션을 그리기 때문. 스윙·ABS 여닫이는 레일 개념 자체가 없으므로 섹션이 통째로 숨는 게 맞다. 그래서 `railHeightOf`는 무레일(0)과 레일 없음(undefined)을 구분한다
- 2026-08-27 · 무레일은 **자동 실내중문에만** 운영 — 팜플렛이 무레일을 자동중문 페이지에만 적었다. 시장에 "하부레일 없는 초슬림 3연동"이 있지만 팜플렛 밖 사양을 임의로 넓히지 않는다(문다온 확인 항목으로 올림). 반대로 하부 표기가 아예 없는 베젤 1.1·커스텀 아치·간살은 3연동 기준(7·20mm)으로 채우고 카드에 `# approx`를 남겼다
- 2026-08-27 · 문짝은 레일 **위**에 얹는다 — 문턱 높이만큼 문짝 높이를 깎고(`H - rail`) 바닥에서 띄운다. 레일을 바닥에 겹쳐 깔기만 하는 안 비채택: 20mm 레일에서 문짝 하단이 문턱을 관통해 보인다. 기존 코드는 `SlidingDoor.tsx`에 7mm가 하드코딩돼 있었고 문짝은 항상 바닥에서 시작했다

- 2026-08-27 · **설정 상수에 `Infinity`를 넣지 않는다 — 큰 유한값을 쓴다.** OrbitControls `maxDistance`를 `Infinity`로 두자 **운영 빌드에서만** 스크롤 줌이 통째로 죽었다(회전은 정상, 콘솔 에러 0). 로컬 dev에서는 `Infinity`·유한값 둘 다 정상이라 dev 검증만으로는 절대 안 잡힌다. 실측: 운영 `Infinity` → 휠에 프레임 변화 0 / 같은 코드 `20` → 정상. **교훈은 값이 아니라 절차다 — dev에서 재현되지 않는 버그가 있으므로, 사용자 조작(줌·회전)을 바꿨으면 운영 빌드에서 한 번 만져봐야 한다**
- 2026-08-27 · 가림(occlusion) 게이트 = **카메라→소품 광선이 개구부를 통과하는가**(`passesOpening`). 이탈각·절두체는 '화면 안인가'만 재서 개구부 벽에 가린 0픽셀 소품을 못 잡았다(우산꽂이·코트·신발 3건 실측). 원점 한 점 근사라 폭 큰 물체는 `wide: true`로 면제 — `offFrame`(프레임 밖)과 **별개 축**으로 둔다. 하나로 겸용했더니 '화면 안인데 면제면 FAIL' 계약이 깨졌다
