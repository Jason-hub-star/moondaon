# PROJECT-STATUS — 문다온 3D 쇼룸

- 갱신: 2026-08-27 (2차 감사 17건 + H1 하부레일 + **실측값 교체 설계 P1~P5** · 번들 `index-0jR_K2nD.js` 운영 검증 완료 · 감사 H1·H3 해소)
- 방향: 중문 디지털트윈 — 고객 커스텀 컨피규레이터 + 영상AI 레퍼런스 캡처, Vite+React+r3f
- 스펙 정본: `docs/archive/GRILL-중문디지털트윈.md` 결정 요약표 + `docs/ref/PATTERN-MAP.md`(패턴 매핑) + `docs/ref/rnd/PATTERN-V2-CONVERGE-LOOP-2026-08-26.md`(v2 수렴)
- 현재 상태: **P1~P11 완료 · 운영 배포 중** — https://moondaon-showroom.vercel.app (Vercel moondaon-showroom, push마다 CI+자동 배포). 패턴 38종·제품 11종·색상 39종·유리 11종·손잡이 3종·하부레일 5종, 게이트 3종(교차검증·CI·SVG 썸네일), 모바일 반응형(QA ?mobile=1). 이력 상세: `STATUS-LOG.md`
- 블로커: 없음
- 대기 항목:
  - **킬-실험 2 (주인님 실행)**: ~/Downloads의 moondaon_*.mp4 + 프롬프트 페어를 영상AI에 레퍼런스 투입 → 프롬프트-only 대비 비교. 결과로 캡처 파이프라인 투자 지속 판정
  - **아이폰 AR 실기기 테스트**: 아이폰 사파리에서 https://moondaon-showroom.vercel.app → "실물 크기로 보기" → 가이드 → Quick Look. 볼 것 3가지 — ① 문이 **벽에** 붙는가(바닥이면 앵커 실패) ② 문 **정면**이 사용자를 보는가(뒤집혔으면 `openAR.ts`의 −Z 규약 지점을 되돌린다) ③ `quickLookCompatible`로 나무결 시트의 타일링이 맞게 나오는가
  - **사업 확인 (배포 후 잔여)**: 팜플렛 사진·문다온 브랜드 웹 게재 권리 — 주인님 지시로 배포 선행. 문제 시 Vercel 비공개 전환으로 즉시 내림
  - **문다온 확인 7건**: **단가표(색상·유리·손잡이·제품·치수별 가격) — 견적 기능의 유일한 착수 블로커. 형식 무관(엑셀·사진·문자) 수령 즉시 카드 스키마로 정리** · S127↔S128 나르본오크 시트코드 · 3연동 겹침폭 실측(현재 30mm approx) · 간살 페이지 라이온월넛 시트코드(3연동 P02 vs 간살 `S1?8` — 팜플렛 인쇄 겹침) · 스윙 "라인반달 300/900"이 3연동 접착식·일체형과 같은 부품인가 · 메탈그레이·실라나스 실물 스와치(현재 사례 사진 픽셀 추정) · 굴곡유리 스와치(팜플렛에 사진 없음)
- 주문가능성 제약 완료 (2026-08-26): 제품 카드 `glassIds`·`colorIds`·`colorCats` allowlist + UI 필터 + 전환 시 자동 보정 + 교차검증 게이트(존재하지 않는 id 차단 — 음성테스트 PASS). 간살 유리5·우드만 / 스윙 유리5·1S 950mm 클램프 / 원슬·베젤 도장3(샴페인골드 카드 신규)+우드 — 실렌더 검증. 스윙 유리 5종은 간살 세트 동일 추정(approx). 테스트에서 공유 링크 주입 우회 발견 → 로드 시 1회 보정으로 차단(운영 재검증 완료)
- 개선 후보(상향): 운영 첫 방문 시 텍스처 로딩 수 초간 검정 → hex 단색 폴백 표시 (P-A에서 텍스처 6장 추가로 추세 악화 — 다음 세션 후보)
- 씬 사실감 트랙 신설 (2026-08-26): 로드맵 `docs/ref/SCENE-PLAN.md`(개선 4축·물건 11개·페이즈 P-A~P-D), 제작 절차는 프로젝트 스킬 `소품`(A/B 등급 판정·img2threejs 함정·이식 규칙). 몬스테라 B안 산출물 `assets/props/monstera/` 보존. SNS 공유는 완료 — 정적 OG 카드 + 스크린샷 공유(`/api/share`·`/s`) 운영 검증됨
- 씬 페이즈루프 완료 (2026-08-26): P-A 재질·빛(CC0 마루·플라스터 노멀·접지그림자) → P-B 현관(신발장·방화문·신발·센서등) → P-C 거실(몬스테라 slim·거울·창+커튼·스탠드조명·월패드, quality 분기) → P-D 생활감(슬리퍼·우산꽂이·잎 곡률). 모바일 프레임 확인. 상세: `docs/ref/SCENE-PLAN.md` 실행 기록
- 도그푸딩 감사 (2026-08-27): 결함 6건 — `docs/status/AUDIT-2026-08-27.md`. **D1(치명, 없는 productId 링크 → 앱 백지)·D5(NaN 씬 붕괴) 수정·배포 완료** (`bce2405`, 운영 번들 `index-ByAVQkBa.js`, 운영 실렌더 확인, CI Node 24 green). 공유 링크 입력 검증 `configurator/shareSchema.ts` 신설 + 테스트 11종(`npm test` — Node 내장 러너, 새 의존성 0). 계약·증거: `docs/goals/GOAL-share-input-validation.md`
- **감사 결함 미결 0건 (2026-08-27 마감)**: D1·D5(공유 링크 입력 검증) · D2(시점 이탈 방지+시점 초기화) · D3(공유 카드 호스트 정확 일치 + 업로드 4중 방어) · D4(부팅 2겹 로딩) — **전부 수정·배포·운영 실측 완료.** D6은 문구 유지 판정(견적 구현으로 맞춤, 단가표 대기). 원본 목록: `AUDIT-2026-08-27.md`
- 신규 팜플렛 반영 (2026-08-27, 페이즈루프 P1~P5): 출처 `문다온업데이트.png` 6p를 24구간 + 경계 18곳 2패스 전수 판독 → 신규 6건·정정 2건만 확정(색상 20종·유리 10종·디바이딩 도형 48종은 이미 반영돼 있었음). **P1** 반달 손잡이 카드 신설 + 손잡이 `colorIds`·`orderType`(교차검증 음성테스트 PASS) · **P2** 굴곡유리 카드 + 유리→손잡이 제약(`requiresIntegratedHandle`, 모루·굴곡 선택 시 일체형 고정 — 실렌더에서 해시가 `integrated`로 자동 보정·칩 1종만 노출 확인) · **P3** swing-2s H2300→**2350**, swing-sidelite 폭 상한 1.6→**1.3m**, 힌지·강화유리 메타를 제품 설명줄에 노출 · **P4** 메탈그레이·실라나스(사례 사진 픽셀 중앙값, approx) · **P5** 문서·결정 3건. 유리 혼합은 비채택(DECISION-LOG)
- 배포 검증 (2026-08-27): `539d019`+`67ccc1f` 운영 반영 확인 — 번들 `index-ByAVQkBa.js` → **`index-DhOCgdNR.js`**. 운영 실측: 라벨 42개 중 잘림 0(높이 24px 단일), 반달 손잡이·굴곡유리·메탈그레이·실라나스 노출, 굴곡유리 클릭 시 손잡이 `integrated` 자동 보정·칩 1종만 잔존, 콘솔 에러 0
- UI 정정 (2026-08-27): 우측 패널 칩 라벨이 `maxWidth 62/76/84px + nowrap`이라 34개 중 14개가 잘렸다. 창 폭 420~1920 전 구간 동일 개수 — 화면 크기 무관(폭이 px 고정). "아치 (스팬드럴 유리/고시)"처럼 앞부분이 같은 이름은 구분 자체가 불가능했다. 2줄 클램프 + `minHeight` 24px(행 정렬 유지)로 잘림 0
- 팜플렛 반영 잔여: 반달 손잡이가 3D에선 사각 바로 근사됨(반원 단면 미구현) · 손잡이 `colorIds`는 메타 전용(3D는 프레임 색 상속, UI 선택 없음) — 둘 다 실물 사진 수령 후 판단
- 씬 포근함 P-E 완료 (2026-08-27, 페이즈루프 P-E1~E6 전 phase PASS): 진단은 "소품 부족"이 아니라 ① P-C가 만든 포근함 소품 5개가 **기본 프레임 밖**이라 0픽셀 기여 ② 프레임 안 최암부가 몬스테라 잎이었다. **프레임 게이트 신설** — `apps/web/src/scene/props.data.ts`(배치 SSOT, three/JSX 의존 0 → node 테스트가 직접 읽음) + `frame.test.ts`(이탈각 총 ≤30°·세로 ≤19°). 카메라도 SSOT(`CAMERA`)로 올려 `App.tsx` 하드코딩 0. 산출: 거울·걸레받이 채도·빈 액자 제거, 소파(짙은 값 앵커)·커튼·담요·신발2켤레·코트·스위치 신설, `ambientLight` 0.55→0.32. **카메라 target y 1.15→0.98** — 카메라 1.5m·세로 반각 22.5°에선 바닥이 3.3m 밖일 때만 프레임에 들어 러그를 어디 깔아도 안 보이는 기하학적 벽이었다(되돌리려면 이 숫자 하나). 계약·증거 `docs/goals/GOAL-scene-cozy.md`, 상세 `docs/ref/SCENE-PLAN.md` P-E
- 씬 P-E 후속 (2026-08-27): ① **커튼 폐기** — 소품 커튼·창 속커튼(sheer) 모두 제거. 저폴리 상자로는 '천'이 안 나오고 벽과 같은 값으로 뭉갰다(주인님 판정) ② **주인님 `?edit=1` 재배치를 정본으로 채택** — 좌표 무수정, 프레임 밖인 소품엔 `offFrame: 'cutoff' | 'light'` 사유만 데이터에 명시 ③ **게이트를 각도 프록시 → 절두체 투영(`frameNdc`/`inFrame`)으로 교체** — 각도 30°가 화면 안(ndc −0.82)인 창을 FAIL로 떨어뜨렸다. 구면 각도로는 직사각 절두체를 못 그린다. 역방향 테스트(화면 안인데 `offFrame`이면 FAIL)로 낡은 면제 표시도 잡는다 ④ **ㄱ자 자동 숨김** — 개방형에서 사라지는 벽(전실 우측벽·개구 우측 벽판)에 걸린 소품을 좌표 규칙(`inRemovedCornerWall`)으로 숨기고 다른 중문에서 복귀. id 열거가 아니라 좌표 판정이라 편집기로 옮겨도 안 낡는다. `npm test` 16/16
- 모바일 소견 (2026-08-27 실측): 레이아웃은 정상 — 세로 분할·캔버스 52vh·칩 2줄 래핑 잘림 0·슬라이더 1줄. 단 **실기기 화면비(≈0.89)에서는 가로 시야가 좁아 창·우측 액자·몬스테라·소파가 프레임 밖**으로 나간다(데스크톱 1.76 기준으로 배치했기 때문). 문은 중앙에 그대로 남아 제품 전달에는 문제없으나, P-E의 포근함 소품 상당수는 사실상 데스크톱 전용이다 — 모바일 전용 카메라(뒤로 빼기 또는 fov 확대)가 후속 후보
- AR 중문화 (2026-08-27): 조사 → 3건 반영. ① **벽 앵커** — three USDZExporter 기본값이 `horizontal`이라 지금까지 중문이 **바닥에 눕고 있었다**. 옵션 유무로 같은 씬을 export해 usda를 대조(`vertical` vs `horizontal`)해 원인 확정. 안드로이드는 `ar-placement="wall"` ② **진입 전 정렬 가이드** — AR 세션 안은 OS 화면이라 우리 UI가 못 들어간다. 실측 폭·높이(예: 폭 1,250mm × 높이 2,300mm) + 3단계 안내를 양 OS 공통으로 표시, 읽는 동안 export를 백그라운드로 굽는다 ③ **인앱브라우저 차단** — `ar/inAppBrowser.ts` 신설(순수 함수, `node --test` 7종). 카톡·라인 자동 탈출, 인스타·네이버는 메뉴 안내. 실렌더 3화면 확인(가이드·model-viewer 문 로드·인앱 안내), `npm test` 23/23, 빌드 green. **미검증: 아이폰 실기기의 실제 벽 부착**(대기 항목 참조)
- 배포 검증 (2026-08-27, AR): `38f824f` 운영 반영 — 번들 `index-VooK5Hcd.js` → **`index-D6tAHp2u.js`**(푸시 20초 내). 운영 실측: AR 버튼 → 가이드 오버레이(폭 1,250mm × 높이 2,300mm 표시), AR 시작 → model-viewer에 문 정상 로드(유리 opacity 스왑 적용·정면 뷰), 콘솔 에러 0
- **D2 해결 + 모바일 카메라 (2026-08-27)**: ① **시점 이탈** — `minPolarAngle` 부재로 세로 스와이프 한 번에 시점이 머리 위로 넘어가 문이 사라졌다. 각도·거리 한계를 `props.data.ts` `ORBIT`에 쌍으로 두고(상한은 **천장**이 정한다: `0.98 + 4.6·cos(69.9°) = 2.56 < 2.645`), 팬은 껐다(타깃이 문을 벗어나면 회전만으로 복구 불가). 실측: 540px 드래그 후 카메라 y가 **2.31**에서 멈춤 ② **복구 버튼** — 기본 시점에서 0.25m 이상 벗어나면 `↺ 시점 초기화` 노출. 구현은 `saveState()`/`reset()`(공식 API). **카메라 position을 직접 써넣는 방식은 매 프레임 도는 `controls.update()`와 싸워 되돌아오지 않았다** — 콘솔 계측으로 확인(`[1.71,2.31,3.22]` → reset → `[1.8,1.5,3.4]`) ③ **모바일 fov 54** — three의 fov는 세로라 화면비 0.89에서 가로 반각이 20.2°(데스크톱 36.1°의 56%)로 깎여 **문 4모서리가 |ndc| 0.90까지 밀렸다**. 카메라를 뒤로 빼는 건 방(깊이 3.5m) 밖이라 불가 → fov만 54로. 문 모서리 **0.73**, 문 화면높이 71%→58%. `npm test` 26/26
- 배포 검증 (2026-08-27, D2+모바일 카메라): `b65c0d9` 운영 반영 — 번들 `index-Drzt_mJI.js`(푸시 30초 내). 운영 실측: 540px 세로 드래그 → 시점 클램프(문 온전히 유지·뒤집힘 없음) → `↺ 시점 초기화` 노출 → 클릭 시 기본 구도 복귀 확인. 모바일 fov 전환은 `?mobile=1`로 확인(뷰 확연히 넓어짐)
- **D3·D4 해결 (2026-08-27)**: **D3** ① `/s`의 호스트 검사가 `endsWith('.public.blob.vercel-storage.com')`이라 **Vercel 고객 전체**의 블롭을 통과시켰다 → 우리 저장소 호스트와 **정확히 일치**해야만 싣도록. 호스트는 R/W 토큰 4번째 세그먼트(`vercel_blob_rw_<storeId>_<secret>`)에서 유도하고, 못 구하면 외부 이미지를 통째로 거부(**fail-closed**). `BLOB_PUBLIC_HOST`로 명시 override 가능 ② `/api/share`에 오리진·형식·크기·빈도 4중 방어 — same-site(Origin, 없으면 Referer) 아니면 403 · JPEG 매직바이트 검사(data URL 접두사는 누구나 붙인다) · 3.5MB→2MB · IP당 10회/10분. 공개 앱이라 진짜 인증은 의미가 없어(누구나 토큰을 받는다) **브라우저에서 우리 사이트를 통해 온 요청만** 받는 쪽으로 잡았다. 레이트리밋은 인스턴스 로컬이라 분산 한도가 아니라 폭주 안전판(`ponytail:` 명시). 회귀 방지 테스트 8종 + CI 스텝 신설(`npm test` at root)
  **배포 후 잡힌 버그 1건**: 토큰 안 store id는 대소문자가 섞여 있는데(`kfpb6CTDHooPslmB`) 실제 blob 호스트는 전부 소문자다(`kfpb6ctdhoopslmb`). `URL.hostname`도 소문자로 정규화되므로 눕히지 않아 **우리 블롭조차 한 번도 일치하지 않았고 공유 카드가 전부 기본 이미지로 떨어졌다.** 운영 검증(우리 호스트로 `/s` 호출 → og.png 폴백)이 잡았고 `toLowerCase()` + `new URL().hostname`과 맞물리는지 보는 계약 테스트로 닫았다
  **D4** 백지 구간을 둘로 나눠 각각 덮는다 — ① 번들(1.1MB) 내려받는 동안은 React가 없으므로 `index.html`의 `#boot`(인라인, 외부 요청 0) ② React는 떴는데 텍스처가 아직인 구간은 `src/Loading.tsx`. 텍스처가 `TextureLoader` 명령형이라 Suspense가 못 잡아 `THREE.DefaultLoadingManager`로 진행률을 받는다. 8초 타임아웃 안전판(로딩 화면에 갇히는 게 백지보다 나쁘다). 둘의 색·문구를 같게 맞춰 교대가 안 보인다
- **견적 트랙 개설 (2026-08-27, 문서상만)**: 주인님 결정 — 견적을 **만들 예정**이라 감사 D6(공유 카드가 없는 기능 약속)은 문구를 낮추지 않고 구현으로 맞춘다. R1-15의 "견적 v1 제외"는 갱신됨(제외 → 후속 범위).
  **현재 상태: 미착수, 자료 대기.** 카드 스키마(색상·유리·패턴·손잡이·제품)에 가격 필드가 **0개**이고 팜플렛에도 가격이 없다(GRILL R1-15에 이미 기록). 단가표를 받기 전엔 금액 계산이 시작되지 않으므로 코드는 손대지 않았다.
  **자료 도착 시 정할 것 3가지** — ① 금액을 화면에 띄우는가, 아니면 문의 폼으로 받는가(중문 업계는 가격 비공개가 흔하다) ② 단가 축이 무엇인가(제품×폭? 색상 등급? 유리 가산?) ③ 시공비·출장비 포함 여부. 범위가 갈리므로 착수 전 `그릴미`로 스펙을 닫는 게 맞다.
- **2차 도그푸딩 감사 (2026-08-27)**: 리플렛 SSOT(`assets/리플렛자료/리플렛_텍스트_정리.json`) ↔ 카드 ↔ 화면 3중 대조. **누락 17건 — `docs/status/AUDIT-2026-08-27-SSOT중문.md`.** 1차 감사가 "앱이 부서지는 결함"이었다면 이번은 **"고객이 결정할 축이 화면에 없다"**. 높음 5건 — H1 하부레일 / H2 문틀 프레임 폭 / H3 5T 강화유리 / H4 "양방향" 스윙이 한 방향만 열림 / H5 패턴 `motions:`가 렌더 방식에 걸려 3연동 디바이딩 7종이 자동·ㄱ자·간살에서 사라지고 간살엔 팜플렛에 없는 아치가 뜬다. Refuted 10건(유리 10종·색상 체계·아치1~7·스윙 제작칫수 등 정상 반영)도 함께 기록해 재조사를 막았다. **"SSOT PDF"는 없다** — HEIC 5장 + 정리 JSON + 팜플렛 PNG가 정본
- **H1 해소 — 하부레일(문턱) 축 신설 (2026-08-27)**: 팜플렛이 제품마다 다르게 운영하는 레일(3연동 7·20mm / 원슬 25×5 / 자동 7·10·무레일)이 카드에도 UI에도 없었다. `data/cards/rails/` **5종 신설** + 제품 `railIds` allowlist + 교차검증 게이트 + 우측 패널 `하부레일 (문턱)` 섹션(선택한 레일의 걸림·외풍·청소 트레이드오프 한 줄) + **3D 문턱 렌더**(문짝이 레일 위에 얹히고, 무레일이면 메시를 안 그린다 — 기존엔 `SlidingDoor.tsx`에 7mm가 하드코딩돼 있었다). 판정은 순수 모듈 `configurator/rails.ts` 하나에만 두고 `rails.test.ts` 7종으로 잠갔다. **`npm test` 33/33 · 빌드 green · 로컬 실렌더 확인**(20mm 문턱 보임 ↔ 무레일 사라짐, 원슬 레일을 3연동에 주입하면 `flat-7`로 보정되고 해시까지 갱신, 전 제품×전 레일 순회 콘솔 에러 0). 힌지 제품(스윙·ABS)은 `railIds` 필드 자체를 두지 않아 섹션이 숨는다
- **배포 검증 (2026-08-27, 하부레일)**: `fa535b5` 운영 반영 — 번들 `index-30e0ZA4I.js` → **`index-B0yxFgDh.js`**(로컬 빌드 hash와 동일, 푸시 후 ~30초). 운영 실측 — ① 제품 11종 전수: 슬라이딩 7종에 `하부레일 (문턱)` 섹션 노출(자동중문만 무레일 포함), 힌지 4종(스윙 3·ABS)은 섹션 자체가 숨음 ② 20mm 선택 시 개구부를 가로지르는 문턱이 그려지고 무레일에서는 사라진다(같은 구도 확대 비교) ③ 공유 링크에 원슬 레일(`slim-25x5`)을 실어 3연동으로 보내면 로드 시 `flat-7`로 보정되고 **해시까지 갱신** ④ 콘솔 에러 0
- **실측값 교체 설계 완료 (2026-08-27, 페이즈루프 P1~P5)**: 목표 — 문다온이 줄자 치수·도면·엑셀을 주면 **어느 카드 어느 숫자인지 즉시 찾아 지워나갈 수 있게**. 조준 판정은 `부분`이었다(R1-14가 이미 "실측치 들어오면 교체"를 결정했고 색상 한 도메인만 실제로 돌아봤다).
  **P1** 카드 스키마에 `measured: [필드…]` + `confirmWith: {필드: 물어볼 내용}` 신설 + 게이트 3종(오타 필드명·`measured`∩`confirmWith` 모순·빈 값) — 음성테스트로 셋 다 차단 확인 · **P2** 제품 11·손잡이 3·레일 5장에 근거 채움 · **P3** 코드에 박혀 있던 수치 4종을 카드로(`overlapM`·`jambM`·`louverBarM`/`louverGapM`) — 값 불변이라 렌더 회귀 0, 실렌더 확인 · **P4** 구간표 → `sizeZonesMm`(감사 M5 해소) · **P5** 패턴 `archProfile` 11장 구조화 + `npm run cards -- --pending` 조회 명령
  **누락 재확인 (같은 날)**: 알려진 문다온 확인 목록 9건을 새 시스템에 대조하니 **6건이 밖에 있었다.** 원인 ① 색상·유리 카드에 `confirmWith`를 안 넣었다("값이 하나뿐이라 기존 등급으로 충분"이 오판 — 시트코드는 hex가 아니라 `hexSource`가 가리킬 수 없다) ② `motion`·`railIds`처럼 자리는 있는데 안 적은 것 2건. 둘 다 보정하고 9/9 잡히는 것을 확인. 코드도 다시 훑어 트랙 피치(`trackPitchM` 33mm)·ABS 문짝 두께(`panelThicknessM` 40mm) 2종을 추가 이관 — **도어 렌더에 남은 문다온 수치 0**
  **결과: 실측 대기 66개 필드 · 근사 카드 81장 · 문다온 확인 71건** — 전부 필드 단위로 세어지고 목록으로 나온다(이전엔 상태문서 산문에 6건). `npm test` 39/40(유일한 red는 타 세션의 가림 게이트) · 빌드 green · oxlint 0 error
- **배포 검증 (2026-08-27, 실측값 교체 설계)**: `dd1da8b`+`6b0c9b8` 운영 반영 — 번들 `index-CV5tGeep.js` → **`index-BmXtj9yB.js`**. 푸시될 트리 기준 `npm test` **40/40**(로컬 red 1건은 타 세션의 미커밋 소품 좌표가 원인이었고, 커밋본에서는 초록임을 확인 후 푸시). 운영 실측 — 제품 11종 전수: 하부레일 자동 3·3연동 계열 2·원슬 1·힌지 4종 0, 구간 라벨은 표가 있는 5종에만, 간살 바·문틀·문턱 실렌더 회귀 0, 콘솔 에러 0
  **운영에서 드러난 것 1건(신규 결함 아님)**: 다른 제품에서 간살로 전환하면 폭 1200mm가 그대로 넘어와 첫 화면이 **"구간 밖 (1200mm)"**으로 뜬다. 팜플렛 간살 표의 1101~1200 공백이 그대로 보이는 것 — 이전에는 간살에 구간 표시 자체가 없어 안 보이던 사실이다. 문다온 확인(`louver.confirmWith.sizeZonesMm`) 결과에 따라 ① 공백이 인쇄 누락이면 표를 메우고 ② 실제 미운영이면 전환 시 가까운 구간으로 스냅한다. 확인 전에는 감추지 않는다
- **H3 강화유리 완료 (2026-08-27, 배포 검증 완료)**: 팜플렛이 유리 페이지마다 반복하는 `5T강화유리 옵션은 별도 (망입은 강화불가)`가 화면에 없었다. `유리 강화 (5T 옵션)` 토글 신설 — 망입 3종은 잠기고 사유가 뜨며, 양개도어(스윙 2S·픽스창)는 항상 켜진 채 잠긴다. **강화 가능 여부는 새 필드 없이 기존 `mesh`에서 파생**(팜플렛이 금지한 건 망입 하나뿐 — 성형 유리는 `confirmWith`로 확인 대기). 판정은 `configurator/tempered.ts` 한 곳, `tempered.test.ts` 6종. 표시는 실제 적용값·저장은 고객 의도라 망입을 거쳐도 의도가 산다. 카드 게이트 신설(강화 기본 제품이 망입 허용 시 FAIL). `npm test` 45/46 · 빌드 green · 실렌더 4경로 + 주입 링크 확인, 콘솔 에러 0
- **배포 검증 (2026-08-27, H3 강화유리)**: `50fd215` 운영 반영 — 번들 `index-BmXtj9yB.js` → **`index-0jR_K2nD.js`**. 푸시 트리 기준 `npm test` **46/46**. 운영 실측 5경로 — ① 강화 켬 → 설명줄에 "5T 강화유리" ② 철망입 전환 → "일반 5mm" + "망입유리(철망)는 강화 처리가 불가능합니다" ③ 브론즈샤틴 복귀 → **강화 살아남음**(의도 보존) ④ 양개도어 → 켜진 채 잠김 + 사유 ⑤ ABS → 섹션 없음. 콘솔 에러 0
- **타 업계 벤치마크 조사 (2026-08-28)**: 8갈래 병렬(창호·자동차·가구인테리어·커스텀소비재·견적리드 + 도면데이터·핫스팟·익명찜비교) → `docs/ref/BENCHMARK-CONFIGURATORS.md`. **국내 창호 5사 중 온라인 시뮬레이터가 실재하는 곳은 2곳뿐이고 1곳(KCC)은 404 비작동** — 벤치마킹 대상이 국내엔 사실상 없어 타 도메인으로 넓힌 판단이 맞았다.
  **채택 6건**(비용순): ①옵션 충돌 시 "왜 안 되는지" 안내(BMW — 우리는 자동 보정하고 침묵 중) ②**견적 = 구성 링크 첨부 폼**(단가 데이터 불필요, 기존 공유 인프라 재사용) ③온보딩 = "우리 집 현관 형태" 3지선다(KKART 실측이 도면 DB 대체) ④부위 클릭 핫스팟(문짝·유리 2개만, 필터일 뿐 게이트 아님) ⑤찜·비교·복귀 = 해시 슬롯(서버·계정·이메일 0) ⑥사람이 읽는 URL 파라미터 병기
  **비채택 7건**(사유는 문서에): 서버 GPU 스트리밍 · 조합별 사전렌더 · 배경 씬 opt-in(중문은 개구부 부착이라 벽 없으면 제품이 안 읽힘) · 아파트 도면 DB(전부 막힘) · 계정 도입 · **실시간 금액 델타**(자동차·PC는 공개단가 표준규격품이라 성립, 중문은 시공비·실측이 껴 조건이 다름 — 단가표를 받아도 자동 채택 아님) · 이메일 매직링크
  **이미 하고 있던 것**: URL 상태 인코딩(Polestar와 동일 구조) · 제약을 목록에서 제거(Therma-Tru) · 로그인 없는 탐색(12곳 전원) · 클라이언트 실시간 3D(자동차 6곳 중 Rivian만 동급)
- 다음 액션: ① **H3 강화유리 옵션** (감사 잔여 중 자체 완결이고 안전 항목) ③ **H5 간살 디자인 좁히기** (팜플렛 3종 명시분은 확인 없이 가능) ④ **아이폰 실기기 AR 확인** (벽 부착·정면 방향·시트 타일링 — 미검증 지점) ⑤ **단가표 수령**(문다온) → 받으면 `그릴미`로 견적 스펙 확정 후 착수 ⑥ 킬-실험 2 결과 수령 ⑦ 문다온 확인 — 기존 6건 + 신규 3건(간살 구간 공백·ㄱ자/자동 디자인 공유·수동 3연동 무레일 운영 여부)
- 씬 후속 후보(낮은 우선순위): 스탠드가 z 2.94로 이동해 화면·벽 워시 모두에서 빠짐 — 빛만 살리려면 앞으로 당겨야 함(주인님 판단 대기) (가림 게이트는 2026-08-27 신설·해결)
